import { Text, View } from "react-native";
import React from "react";

const Today = () => {
    return (
        <View>
            <Text>today</Text>
        </View>
    );
};

export default Today;
